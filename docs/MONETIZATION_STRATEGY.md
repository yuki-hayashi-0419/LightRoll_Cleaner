# マネタイズ戦略・広告配置計画

**策定日**: 2025年12月25日
**対象アプリ**: LightRoll Cleaner
**戦略**: 価値実感 → 即座課金誘導モデル

---

## 目次

1. [全体戦略](#1-全体戦略)
2. [ユーザー体験フロー](#2-ユーザー体験フロー)
3. [広告配置計画](#3-広告配置計画)
4. [Free→Pro誘導戦略](#4-freepro誘導戦略)
5. [実装計画](#5-実装計画)
6. [KPI・効果測定](#6-kpi効果測定)

---

## 1. 全体戦略

### 1.1 基本方針

**「Try & Lock」モデル**

1. **Try（価値実感）**: 初回利用で完全な価値を体験
2. **Lock（課金誘導）**: 2回目以降の利用で即座に課金が必要

### 1.2 収益源

| 収益源 | 割合目標 | 説明 |
|--------|----------|------|
| **サブスクリプション** | 70% | Pro版の月額・年額プラン |
| **買い切り** | 20% | Lifetimeプラン |
| **広告収益** | 10% | Free版ユーザーからの広告収入 |

### 1.3 ターゲット指標

- **Pro版コンバージョン率**: 15-25%
- **初回体験完了率**: 80%以上
- **広告ARPDAU（Free版）**: $0.05-0.10
- **LTV（Pro版ユーザー）**: $30-50

---

## 2. ユーザー体験フロー

### 2.1 初回起動〜初回削除（価値実感フェーズ）

```
📱 アプリ起動
  ↓
🔐 写真ライブラリ権限許可
  ↓
🎉 ウェルカム画面
   「重複写真を検出して、ストレージを解放しましょう！」
   [スキャンを開始]
  ↓
🔍 初回スキャン（完全無制限）
   - 全ての写真をスキャン
   - 重複・ブレを全て検出
   - 広告なし（体験を邪魔しない）
  ↓
📊 スキャン結果表示
   「500枚の重複写真を検出！」
   「300枚のブレた写真を発見！」
   「約5GBのストレージを解放できます」

   💡 無料で50枚まで削除できます

   [50枚削除する（無料）]
   [Pro版で全て削除（$3/月）]
  ↓
🗑️ 初回削除（50枚まで無料）
   - 実際にストレージ解放を体験
   - 削除完了時: インタースティシャル広告
  ↓
✅ 削除完了画面
   「50枚削除しました！」
   「約500MB解放」

   でも、まだ...
   残り450枚の重複写真があります

   [Pro版で残りも削除]
   [後で]
```

**所要時間**: 5-10分
**目的**: アプリの価値を完全に理解してもらう

---

### 2.2 2回目以降の利用（課金誘導フェーズ）

```
📱 アプリ起動（2回目）
  ↓
🚫 制限到達画面
   「無料版では既に50枚削除しました」

   さらに削除するには...

   【Pro版の特典】
   ✅ 無制限削除
   ✅ 広告なし
   ✅ 自動スキャン（毎日/週1/月1）
   ✅ 高度な分析設定

   [7日間無料で試す]
   [料金プランを見る]
   [閉じる]
```

**目的**: 「もっと使いたい！」という気持ちを課金に繋げる

---

## 3. 広告配置計画

### 3.1 広告種類と配置場所

#### 📌 バナー広告（Banner Ad）

| 配置場所 | タイミング | サイズ | 頻度 |
|---------|-----------|--------|------|
| **ホーム画面下部** | 常時表示 | 320x50 | 常時 |
| **スキャン結果画面下部** | 結果表示時 | 320x50 | 常時 |
| **設定画面下部** | 設定閲覧時 | 320x50 | 常時 |

**実装状態**: ✅ 既に実装済み（`BannerAdView.swift`）

**最適化施策**:
- Pro版購入後は即座に非表示
- 画面遷移時のバナー再読み込み最適化
- 広告読み込み失敗時のフォールバック実装

---

#### 🎬 インタースティシャル広告（Interstitial Ad）

| 配置場所 | タイミング | 頻度制限 | 優先度 |
|---------|-----------|----------|--------|
| **削除完了後** | 初回削除完了時 | 1回/セッション | 高 |
| **スキャン完了後** | 2回目以降のスキャン完了時 | 1回/セッション | 中 |
| **アプリ再起動時** | 3回目以降の起動時 | 1日1回まで | 低 |

**実装状態**: ❌ 未実装

**実装優先度**: 🔴 高（M10で実装推奨）

**実装方針**:
```swift
// AdInterstitialManager.swift（新規作成）
@MainActor
class AdInterstitialManager: ObservableObject {
    @Published var isReady: Bool = false
    private var interstitialAd: GADInterstitialAd?
    private var lastShowTime: Date?

    // 1セッションに1回まで制限
    private var hasShownThisSession = false

    func loadAd() { ... }

    func showIfReady(
        from viewController: UIViewController,
        onDismiss: @escaping () -> Void
    ) {
        // 頻度制限チェック
        guard !hasShownThisSession else { return }
        guard canShowAd() else { return }

        // 表示
        interstitialAd?.present(fromRootViewController: viewController)
        hasShownThisSession = true
        lastShowTime = Date()
    }

    private func canShowAd() -> Bool {
        // 最後の表示から30分以上経過していること
        guard let lastShow = lastShowTime else { return true }
        return Date().timeIntervalSince(lastShow) > 1800
    }
}
```

---

#### 📰 ネイティブ広告（Native Ad）

| 配置場所 | タイミング | 優先度 |
|---------|-----------|--------|
| **グループリスト内** | 3つに1つ | 低 |
| **スキャン結果リスト内** | 5つに1つ | 低 |

**実装状態**: ❌ 未実装

**実装優先度**: 🟡 中（M11以降で検討）

**メリット**:
- ユーザー体験を邪魔しない
- クリック率が高い

**デメリット**:
- 実装工数が大きい（2-3時間）
- 収益インパクトは小さい

**判断**: M10では見送り、M11以降でA/Bテストを実施

---

### 3.2 広告表示ルール

#### Free版での広告表示

```
✅ 表示する場面:
- ホーム画面（バナー）
- スキャン結果画面（バナー）
- 削除完了後（インタースティシャル、1回/セッション）
- アプリ再起動時（インタースティシャル、1日1回）

❌ 表示しない場面:
- 初回スキャン中
- 初回削除中
- 権限リクエスト画面
- 重要なエラー画面
- Pro版購入フロー内
```

#### Pro版での広告表示

```
❌ 一切表示しない
- isPremium == true の場合、全ての広告を非表示
- BannerAdView は .opacity(isPremium ? 0 : 1)
- インタースティシャルは表示スキップ
```

---

### 3.3 広告収益最適化戦略

#### A/Bテスト項目

| テスト項目 | パターンA | パターンB | 測定指標 |
|-----------|----------|----------|----------|
| インタースティシャル頻度 | 1回/セッション | 2回/セッション | ARPDAU vs 離脱率 |
| バナー位置 | 画面下部 | 画面上部 | クリック率 |
| 削除完了後広告 | 即座に表示 | 3秒待ってから表示 | クリック率 vs UX評価 |

#### 広告ユニットID管理

```swift
// AdConfiguration.swift（既存ファイルを拡張）
enum AdUnitID {
    static let homeBanner = "ca-app-pub-XXXXX/BANNER_HOME"
    static let resultBanner = "ca-app-pub-XXXXX/BANNER_RESULT"
    static let deleteInterstitial = "ca-app-pub-XXXXX/INTERSTITIAL_DELETE"
    static let appOpenInterstitial = "ca-app-pub-XXXXX/INTERSTITIAL_OPEN"

    // テスト用
    #if DEBUG
    static let testBanner = "ca-app-pub-3940256099942544/2934735716"
    static let testInterstitial = "ca-app-pub-3940256099942544/4411468910"
    #endif
}
```

---

## 4. Free→Pro誘導戦略

### 4.1 誘導ポイント（Paywall表示タイミング）

#### 🎯 Primary Paywall（最優先）

**タイミング**: 初回削除完了後（50枚削除）

```swift
// DeletePhotosUseCase.swift 内
if !isPremium && totalDeletedCount >= 50 {
    // 制限到達
    showLimitReachedSheet = true

    // Analytics記録
    Analytics.logEvent("free_limit_reached", parameters: [
        "deleted_count": totalDeletedCount,
        "remaining_duplicates": remainingCount,
        "freed_space_mb": freedSpaceMB
    ])
}
```

**表示内容**:
```
✅ 50枚削除しました！
約500MB解放

でも、まだ...
残り450枚の重複写真
残り4.5GBの解放可能容量

【Pro版なら】
✅ 無制限削除
✅ 広告なし
✅ 自動スキャン

月額 $3 | 年額 $20（50%OFF）| 買い切り $30

[7日間無料で試す]
[料金プランを見る]
[後で]
```

**コンバージョン目標**: 20-30%

---

#### 🎯 Secondary Paywall（補助的）

**タイミング**: 2回目のスキャン試行時

```
🚫 無料版では1回のみスキャンできます

すでに以下を検出済みです：
- 重複写真: 500枚
- ブレた写真: 300枚

【Pro版なら】
✅ 毎日自動スキャン
✅ 新しい重複を自動検出
✅ 常に最新の状態を維持

[Pro版にアップグレード]
[閉じる]
```

**コンバージョン目標**: 10-15%

---

#### 🎯 Tertiary Paywall（補完的）

**タイミング**:
- 設定画面の「Premium」ボタンタップ時
- ホーム画面の「Pro版を試す」バナータップ時

**コンバージョン目標**: 5-10%

---

### 4.2 無料トライアル設計

#### 7日間無料トライアル

**対象プラン**:
- 月額プラン（$3/月）のみ
- 年額・買い切りは対象外

**実装**:
```swift
// ProductIdentifiers.swift 修正
extension ProductIdentifier {
    public var freeTrialDays: Int? {
        switch self {
        case .monthlyPremium:
            return 7  // 7日間無料
        case .yearlyPremium, .lifetimePremium:
            return nil
        }
    }
}
```

**トライアル中の表示**:
```
【無料トライアル中】
残り5日間

トライアル終了後: $3/月
いつでもキャンセル可能

[設定]
```

**トライアル終了3日前の通知**:
```
【トライアル終了まであと3日】

LightRoll Cleaner Pro

3日後に月額$3が課金されます。
不要な場合は設定からキャンセルできます。

[設定を開く]
[OK]
```

---

### 4.3 料金プラン提示方法

#### おすすめ順の表示

```
┌─────────────────────────────┐
│  🏆 年額プラン（推奨）        │
│  $20/年（約50%割引）         │
│  月額換算 約$1.67            │
│                             │
│  ✅ 無制限削除               │
│  ✅ 広告なし                 │
│  ✅ 自動スキャン             │
│                             │
│  [選択]                     │
└─────────────────────────────┘

┌─────────────────────────────┐
│  月額プラン                  │
│  $3/月                      │
│  🎁 7日間無料トライアル      │
│                             │
│  [選択]                     │
└─────────────────────────────┘

┌─────────────────────────────┐
│  買い切りプラン              │
│  $30（一度きり）             │
│                             │
│  [選択]                     │
└─────────────────────────────┘
```

#### 価格アンカリング効果

```
従来の方法:
毎月写真を手動で整理: 時給$10 × 2時間 = $20/月
年間: $240

LightRoll Cleaner Pro:
年額わずか $20（92%節約）
```

---

### 4.4 Pro版の価値強調

#### 定量的価値

```
【無料版で体験したこと】
✅ 500枚の重複を検出（50枚のみ削除）
✅ 300枚のブレた写真を発見（削除不可）
✅ 約500MB解放（可能性の10%のみ）

【Pro版で得られる価値】
✅ 残り450枚の重複を削除
✅ 300枚のブレた写真を削除
✅ 合計 約5GB解放
✅ 月額わずか $3（コーヒー1杯分）
```

#### 定性的価値

```
【Free版の課題】
😫 広告が邪魔
😫 削除制限でストレスフル
😫 手動スキャンが面倒

【Pro版の快適さ】
😊 広告なしでスッキリ
😊 無制限に削除できる安心感
😊 自動スキャンで手間いらず
```

---

## 5. 実装計画

### 5.1 Phase 1: 基本制限実装（M10リリース前）

**優先度**: 🔴 最高

| タスク | 内容 | 工数 | 担当 |
|--------|------|------|------|
| スキャン回数制限 | 初回1回のみ許可 | 1.5h | Developer |
| 削除枚数制限修正 | 1日50枚→合計50枚に変更 | 1h | Developer |
| LimitReachedSheet改善 | Pro誘導強化 | 1.5h | Developer |
| インタースティシャル広告実装 | 削除完了時に表示 | 2h | Developer |
| 無料トライアル設定 | 7日間トライアル | 1h | Developer |

**合計工数**: 約7時間（1日作業）

---

### 5.2 Phase 2: 広告最適化（M10リリース後）

**優先度**: 🟡 中

| タスク | 内容 | 工数 |
|--------|------|------|
| A/Bテスト基盤構築 | Firebase Remote Config | 2h |
| 広告頻度最適化 | インタースティシャル調整 | 1h |
| ネイティブ広告検討 | 実装可否判断 | 1h |

**合計工数**: 約4時間

---

### 5.3 Phase 3: 高度な誘導施策（M11以降）

**優先度**: 🟢 低

| タスク | 内容 | 工数 |
|--------|------|------|
| パーソナライズド価格 | ユーザー行動に応じた価格提示 | 4h |
| リテンション施策 | 解約防止フロー | 3h |
| リファラルプログラム | 友達紹介で特典 | 4h |

---

### 5.4 実装詳細

#### 5.4.1 スキャン回数制限

**新規ファイル**: `ScanLimitManager.swift`

```swift
import Foundation

/// スキャン回数制限管理
@MainActor
@Observable
public final class ScanLimitManager {
    /// UserDefaultsキー
    private enum Keys {
        static let hasScannedBefore = "has_scanned_before"
        static let firstScanDate = "first_scan_date"
    }

    /// 初回スキャン済みかどうか
    public private(set) var hasScannedBefore: Bool {
        didSet {
            UserDefaults.standard.set(hasScannedBefore, forKey: Keys.hasScannedBefore)
        }
    }

    /// 初回スキャン日時
    public private(set) var firstScanDate: Date? {
        didSet {
            UserDefaults.standard.set(firstScanDate, forKey: Keys.firstScanDate)
        }
    }

    public init() {
        self.hasScannedBefore = UserDefaults.standard.bool(forKey: Keys.hasScannedBefore)
        self.firstScanDate = UserDefaults.standard.object(forKey: Keys.firstScanDate) as? Date
    }

    /// スキャン可能かどうか判定
    /// - Parameter isPremium: Pro版かどうか
    /// - Returns: スキャン可能ならtrue
    public func canScan(isPremium: Bool) -> Bool {
        // Pro版は無制限
        if isPremium {
            return true
        }

        // Free版は初回のみ
        return !hasScannedBefore
    }

    /// スキャン完了を記録
    public func recordScan() {
        if !hasScannedBefore {
            hasScannedBefore = true
            firstScanDate = Date()
        }
    }

    /// デバッグ用: リセット
    #if DEBUG
    public func reset() {
        hasScannedBefore = false
        firstScanDate = nil
        UserDefaults.standard.removeObject(forKey: Keys.hasScannedBefore)
        UserDefaults.standard.removeObject(forKey: Keys.firstScanDate)
    }
    #endif
}
```

---

#### 5.4.2 削除枚数制限修正

**既存ファイル修正**: `PremiumManager.swift`

```swift
// 変更前: 1日あたりの制限
private static let freeDailyLimit = 50
public private(set) var dailyDeleteCount: Int = 0

// 変更後: 合計での制限
private static let freeTotalLimit = 50
public private(set) var totalDeleteCount: Int = 0  // 名前変更

// canDelete修正
public func canDelete(count: Int) -> Bool {
    if isPremium {
        return true
    }

    // Free版は合計50枚まで
    return totalDeleteCount + count <= Self.freeTotalLimit
}

// incrementDeleteCount修正
public func incrementDeleteCount(_ count: Int) {
    totalDeleteCount += count  // dailyではなくtotal
}

// resetDailyCount削除（不要になる）
```

**UserDefaults永続化**:

```swift
private enum Keys {
    static let totalDeleteCount = "total_delete_count_free"
}

public init(purchaseRepository: any PurchaseRepositoryProtocol) {
    self.purchaseRepository = purchaseRepository

    // 永続化されたカウントを読み込み
    self.totalDeleteCount = UserDefaults.standard.integer(forKey: Keys.totalDeleteCount)
}

public func incrementDeleteCount(_ count: Int) {
    totalDeleteCount += count
    UserDefaults.standard.set(totalDeleteCount, forKey: Keys.totalDeleteCount)
}
```

---

#### 5.4.3 インタースティシャル広告実装

**新規ファイル**: `AdInterstitialManager.swift`

```swift
import GoogleMobileAds
import UIKit

/// インタースティシャル広告管理
@MainActor
@Observable
public final class AdInterstitialManager: NSObject {
    /// 広告が読み込まれているか
    public private(set) var isReady: Bool = false

    /// インタースティシャル広告
    private var interstitialAd: GADInterstitialAd?

    /// 最後に表示した日時
    private var lastShowTime: Date?

    /// このセッションで既に表示したか
    private var hasShownThisSession = false

    /// Pro版かどうか
    private let isPremium: () -> Bool

    public init(isPremium: @escaping () -> Bool) {
        self.isPremium = isPremium
        super.init()
    }

    /// 広告を事前読み込み
    public func loadAd() {
        // Pro版では読み込まない
        guard !isPremium() else { return }

        let request = GADRequest()
        GADInterstitialAd.load(
            withAdUnitID: AdUnitID.deleteInterstitial,
            request: request
        ) { [weak self] ad, error in
            guard let self else { return }

            if let error {
                print("❌ インタースティシャル広告読み込みエラー: \(error.localizedDescription)")
                self.isReady = false
                return
            }

            self.interstitialAd = ad
            self.interstitialAd?.fullScreenContentDelegate = self
            self.isReady = true
            print("✅ インタースティシャル広告読み込み完了")
        }
    }

    /// 広告を表示（条件チェック付き）
    public func showIfReady(from viewController: UIViewController) {
        // Pro版では表示しない
        guard !isPremium() else { return }

        // 既にこのセッションで表示済み
        guard !hasShownThisSession else {
            print("ℹ️ このセッションで既に表示済み")
            return
        }

        // 最後の表示から30分以内
        if let lastShow = lastShowTime,
           Date().timeIntervalSince(lastShow) < 1800 {
            print("ℹ️ 表示間隔制限（30分以内）")
            return
        }

        // 広告が準備できていない
        guard isReady, let ad = interstitialAd else {
            print("⚠️ 広告が準備できていません")
            return
        }

        // 表示
        ad.present(fromRootViewController: viewController)
        hasShownThisSession = true
        lastShowTime = Date()
    }

    /// セッション終了時にリセット
    public func resetSession() {
        hasShownThisSession = false
    }
}

// MARK: - GADFullScreenContentDelegate

extension AdInterstitialManager: GADFullScreenContentDelegate {
    public func adDidDismissFullScreenContent(_ ad: GADFullScreenPresentingAd) {
        print("✅ インタースティシャル広告が閉じられました")
        isReady = false

        // 次の広告を事前読み込み
        loadAd()
    }

    public func ad(_ ad: GADFullScreenPresentingAd, didFailToPresentFullScreenContentWithError error: Error) {
        print("❌ インタースティシャル広告表示エラー: \(error.localizedDescription)")
        isReady = false
    }
}
```

**使用例**:

```swift
// DeletePhotosUseCase.swift 内
if deletedCount > 0 {
    // 削除完了後にインタースティシャル広告を表示
    await adInterstitialManager.showIfReady(from: viewController)
}
```

---

## 6. KPI・効果測定

### 6.1 追跡すべき指標

#### ユーザー行動

| KPI | 目標値 | 測定方法 |
|-----|--------|----------|
| 初回体験完了率 | 80% | スキャン→削除まで完了 |
| Free版平均利用時間 | 5-10分 | Analytics |
| Free版離脱率 | <30% | 初回削除前の離脱 |

#### 収益指標

| KPI | 目標値 | 測定方法 |
|-----|--------|----------|
| Pro版コンバージョン率 | 15-25% | 購入/DAU |
| ARPDAU（Free版） | $0.05-0.10 | 広告収益/DAU |
| ARPU（全体） | $0.50-1.00 | (広告+課金)/MAU |
| LTV（Pro版） | $30-50 | 生涯収益 |

#### 広告効果

| KPI | 目標値 | 測定方法 |
|-----|--------|----------|
| バナー広告CTR | 1-3% | AdMob |
| インタースティシャルCTR | 5-10% | AdMob |
| 広告Fill Rate | >95% | AdMob |

---

### 6.2 A/Bテスト計画

#### テスト1: 削除制限枚数

- **パターンA**: 50枚まで無料
- **パターンB**: 30枚まで無料
- **パターンC**: 100枚まで無料

**測定**: コンバージョン率、離脱率

---

#### テスト2: インタースティシャル表示タイミング

- **パターンA**: 削除完了直後
- **パターンB**: 削除完了3秒後
- **パターンC**: 表示しない

**測定**: 広告収益、ユーザー満足度

---

#### テスト3: 料金プラン提示順

- **パターンA**: 年額（推奨）→月額→買い切り
- **パターンB**: 月額（トライアル付き）→年額→買い切り
- **パターンC**: 買い切り→年額→月額

**測定**: 各プランのコンバージョン率、総収益

---

### 6.3 Analytics実装

**必要なイベント**:

```swift
// 初回スキャン開始
Analytics.logEvent("first_scan_started", parameters: nil)

// 初回スキャン完了
Analytics.logEvent("first_scan_completed", parameters: [
    "duplicates_found": duplicateCount,
    "blurry_found": blurryCount,
    "freeable_space_mb": freeableSpaceMB
])

// 初回削除完了
Analytics.logEvent("first_delete_completed", parameters: [
    "deleted_count": deletedCount,
    "freed_space_mb": freedSpaceMB
])

// 制限到達
Analytics.logEvent("free_limit_reached", parameters: [
    "total_deleted": totalDeletedCount,
    "remaining_duplicates": remainingCount
])

// Paywall表示
Analytics.logEvent("paywall_shown", parameters: [
    "trigger": "limit_reached" // or "second_scan", "settings"
])

// Pro版購入
Analytics.logEvent("pro_purchased", parameters: [
    "plan": "monthly" // or "yearly", "lifetime"
    "price": priceValue,
    "trial": hasFreeTrial
])

// 広告表示
Analytics.logEvent("ad_shown", parameters: [
    "type": "interstitial" // or "banner"
    "placement": "delete_complete"
])
```

---

## 7. リスク管理

### 7.1 想定リスクと対策

| リスク | 影響度 | 対策 |
|--------|--------|------|
| コンバージョン率が低い | 高 | 削除制限を緩和（50→100枚） |
| 広告収益が低い | 中 | ネイティブ広告追加 |
| Free版離脱率が高い | 高 | 初回体験を改善 |
| トライアル解約率が高い | 中 | トライアル中の価値提供強化 |
| App Store審査却下 | 高 | 削除制限を明確に表示 |

---

### 7.2 App Store審査対策

#### ガイドライン準拠

- **3.1.1 In-App Purchase**: サブスクリプションはStoreKit経由
- **3.1.3(a) Reader Apps**: 該当なし（コンテンツ消費アプリではない）
- **2.3.8 Metadata**: 機能制限を明記
- **5.1.2 Data Use**: 広告IDの使用目的を明記

#### 必須表示

```
設定画面 > Premium:
「Free版では合計50枚まで削除できます」
「Pro版では無制限に削除できます」

初回削除完了画面:
「Free版: 50枚削除完了」
「さらに削除するにはPro版が必要です」
```

---

## 8. まとめ

### 実装優先順位

1. **Phase 1（M10必須）**: 基本制限 + インタースティシャル広告
2. **Phase 2（M10.1）**: A/Bテスト基盤 + 広告最適化
3. **Phase 3（M11）**: 高度な誘導施策

### 次のアクション

1. ✅ この戦略ドキュメントをレビュー
2. ⬜ Phase 1の実装開始（7時間）
3. ⬜ テスト環境での動作確認
4. ⬜ App Store審査用のスクリーンショット準備
5. ⬜ M10リリース

---

**策定者**: AI Assistant
**レビュー**: Yuki Hayashi
**次回更新予定**: M10リリース後（効果測定結果を反映）
